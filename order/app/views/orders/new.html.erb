<%= form_for :order, url:orders_path(@order.masa_no)  do |f| %>

    <% if @order.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@order.errors.count, "error") %> prohibited
            this order from being saved:</h2>
          <ul>
            <% @order.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :sipariş %><br>
      <%= f.text_field :order %>
    </p>

    <p>
      <%= f.label :Fiyat %><br>
      <%= f.text_area :price %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>